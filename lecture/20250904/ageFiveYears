#include <iostream>
//#include <string>

int main(){

    int age{};
    std::cout << "Enter your age: ";
    std::cin >> age;

    std::cout << "Five years from now you will be : " << age + 5 << std::endl;
    return 0; 
}

/*
If Statements in c++
are used to make decisions in code. They allow you to execute certain blocks of code based on whether a condition is true or false.
They are fundamental for controlling the flow of a program and enabling conditional logic.
Example:

    if (string traffic light equals "green") {
        
    } else(check with "orange") {
        std::cout << "You are a minor." << std::endl;
    }else{
        stop
    }







*/